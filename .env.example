# Environment Configuration for Copilot Quant
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# OpenAI API Configuration (for LLM-based strategy generation)
# Get your API key from https://platform.openai.com/api-keys
# This feature is for INTERNAL USE ONLY
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Specify which model to use (default: gpt-3.5-turbo)
# OPENAI_MODEL=gpt-3.5-turbo

# Optional: Rate limiting (requests per hour, default: 20)
# LLM_RATE_LIMIT=20

# Database Configuration (if applicable)
# DATABASE_URL=postgresql://user:password@localhost/copilot_quant

# Broker API Keys (for live trading - not currently used)
# IB_HOST=127.0.0.1
# IB_PORT=7497
# IB_CLIENT_ID=1

# Data Provider API Keys
# ALPHA_VANTAGE_API_KEY=your-key-here
# POLYGON_API_KEY=your-key-here

# Application Configuration
# SECRET_KEY=your-secret-key-for-sessions
# DEBUG=False
